{
  "name": "kinship-auto-layout",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic auto-layout engine for printable family trees",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -w @halfsweetlemon/kinship-auto-layout-core build",
    "test": "npm run -w @halfsweetlemon/kinship-auto-layout-core test",
    "demo": "npm run -w @halfsweetlemon/kinship-auto-layout-demo dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
